{{> page/navbar }}

<h2 class="h2-break">🏬 Lista mieszkań</h2>
<hr class="mt-0 mb-4">

<div class="text-center">
    {{#if (equals userRole "Admin")}}
        <div class="col-md-12 text-end" style="margin-bottom: 10px">
            <button class="btn btn-gold btn-space-list">Oceń zaznaczone (GPT)</button>
            <button class="btn btn-red btn-space-list" onclick="deleteRecords('flats', selectedRecordsIDs())">Usuń
                zaznaczone
            </button>
        </div>
    {{/if}}

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <tr>
                <th>Lp.</th>
                <th>Numer oferty</th>
                <th>Typ oferty</th>
                <th>Status oferty</th>
                <th>Status w bazie (ocena)</th>
                <th>Ocenione przez</th>
                <th>Ocena AI</th>
                <th>Do usunięcia</th>
                {{#if (equals userRole "Admin")}}
                    <th onclick="selectAllCheckboxes(this)">Zaznacz</th>{{/if}}
            </tr>
            {{#each flatsList}}
                <tr>
                    <td>{{this.flatNumber}}.</td>
                    <td><a class="rer-link" href="/flats/{{this.flatNumber}}">{{this.offerId}}</a></td>
                    <td>{{this.offerType}}</td>
                    <td>{{this.offerStatus}}</td>
                    <td>{{convertBoolToEmoji this.rateStatus}}</td>
                    <td>
                        <div style="color: #152e52; font-style: italic;">
                            {{convertFalseToBlackSquare this.user}}
                        </div>
                    </td>
                    <td>{{convertBoolToEmoji this.status}}</td>
                    <td>{{convertBoolToEmoji this.delete}}</td>
                    {{#if (equals userRole "Admin")}}
                        <td>
                            <input class="form-check-input" type="checkbox" id="check" name="check" value="{{this.id}}">
                        </td>
                    {{/if}}
                </tr>
            {{/each}}

        </table>
    </div>
</div>
<script src="/assets/js/record-lists/select-all-checkboxes.js"></script>
<script src="/assets/js/record-lists/delete-selected-records.js"></script>